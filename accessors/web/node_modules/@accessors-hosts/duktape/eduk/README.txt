$Id$

This directory contains a version of Duktape that can run on an embedded system.

In particular, it does not depend on the filesystem to find the
duktapeHost.js and other js files.

See https://www.icyphy.org/accessors/wiki/Main/DuktapeHost


