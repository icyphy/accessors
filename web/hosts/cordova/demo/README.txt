To create a new Cordova App with name "AppName", run:

$ ./createApp AppName

The default build file and README.txt are:
.build.xml
.README.txt

Change these, and install them for all demos using the command:

$ ./updateAnt

To clean up all the demos, run:

$ ./cleanAll
